import "../styles/theories/07_AtRoot.scss";

export default function AtRoot() {
	return (
		<>
			<div className="at-root-frame">
				<h1 className="heading">@at-root</h1>
			</div>
			<h1 className="heading">@at-root</h1>
			<h4>@at-root: 클래스를 중첩 밖(root)으로 내보내는 키워드</h4>
		</>
	);
}
